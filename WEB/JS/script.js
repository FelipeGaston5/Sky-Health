// Script geral do sistema
document.addEventListener('DOMContentLoaded', () => {
    console.log("Script geral carregado.");
    // Exemplo: Dark mode toggle
    const toggleDark = () => {
        document.body.classList.toggle('dark-mode');
    };
    // Adicionar botão ou lógica para dark mode aqui
});
